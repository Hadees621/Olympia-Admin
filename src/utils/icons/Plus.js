import React from "react";

const Plus = ({ classes }) => {
  return (
    <img
      src="/assets/logomark.png"
      alt="logo"
      className={`w-[200px] align-middle ${classes}`}
    />
  );
};

export default Plus;
